[package]
name = "tokio_echo"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.49", features = ["rt", "rt-multi-thread", "net", "io-util", "time", "sync", "macros"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
